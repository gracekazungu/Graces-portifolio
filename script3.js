let email=new Typed('.email1',{
    strings:['Email'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})
let num=new Typed('.num',{
    strings:['Number'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})
let meee=new Typed('.meee',{
    strings:['Me'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})