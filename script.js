// Typing Effect

let there=new Typed('.there',{
    strings:['there!,'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})

let me=new Typed('.me',{
    strings:['me!,'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})

let dev=new Typed('.dev',{
    strings:['Develpoer'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})
let entry=new Typed('.entry',{
    strings:['Entry Level'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})

