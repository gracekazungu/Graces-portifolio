let html=new Typed('.html',{
    strings:['HTML/CSS and Javascript'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})
let python=new Typed('.python',{
    strings:['python'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})
let photo=new Typed('.photo',{
    strings:['Illustrater and Photoshop'],
    typedSpeed:200,
    backSpeed:200,
    backDelay:200,
    loop:true
})
